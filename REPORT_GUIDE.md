# 小组汇报完整版大纲（新能源小车项目）

> 适用场景：老师可能中途打断、跳页、追问代码实现细节。  
> 使用方式：主讲按“完整版”准备；现场按“精简版路径”灵活跳转。

---

## A. 先给你一页“总导航”（建议放第2页）

- 我们的汇报有 **4 层结构**：
  1) 问题与约束（为什么难）
  2) 系统与算法（怎么做）
  3) 实验与结果（做得怎样）
  4) 工程价值与边界（能走多远）
- 若老师时间紧：直接跳到 **算法对比页 + 结果页 + 亮点页**。

---

## B. PPT 完整版（建议 20~24 页）

## 0. 封面（1页）
- 项目名、组员、分工、日期。
- 一句话定位：**“面向新能源物流调度的充电约束智能派单仿真系统”**。

## 1. 任务背景与评分规则（2页）

### 第1页：课程要求映射
- 车辆有限、电量上限、载重上限。
- 动态任务随机出现。
- 完成早 + 路径短得分高，超时扣分。
- 需要考虑充电站排队与负载。
- 至少两种策略，最好有进阶算法与可视化。

### 第2页：我们如何逐条满足要求
- “要求 -> 对应模块/代码”的映射表（建议用两列表格）。
- 示例：
  - 动态任务：Poisson 任务生成（`simulator`）
  - 两种以上策略：Nearest/Largest/RL（`scheduler`）
  - 评分：`scoring` 模块统一打分
  - 图搜索：`graph + shortest_path`

## 2. 问题建模（2页）

### 第3页：系统对象
- Vehicle：位置、电量、载重、状态、充电状态。
- Task：创建时间、截止时间、重量、剩余重量、状态。
- ChargingStation：桩容量、排队队列、等待估计。
- Graph：节点与边，支持最短路。

### 第4页：我们优化的核心目标
- 主目标：任务完成质量（完成率+时效+路径质量）。
- 次目标：系统稳定运行（减少“半路没电”、降低充电拥堵）。
- 用评分函数聚合多目标（时间、距离、载重匹配、多车协同奖励）。

## 3. 系统架构（2页）

### 第5页：架构图（总览）
- 输入：场景参数、图结构、策略选择。
- 循环：任务生成 -> 派单 -> 车辆执行 -> 充电 -> 评分更新。
- 输出：任务状态、车辆状态、总分、可视化展示。

### 第6页：模块职责拆解
- `graph/`：图与最短路径。
- `model/`：任务/车辆数据结构。
- `scheduler/`：策略接口+三种策略实现。
- `charging/`：充电站选择、排队、充电推进。
- `simulator/`：时间推进、任务生成、状态更新。
- `scoring/`：统一评分指标。
- `app/`：GUI 展示。

## 4. 核心技术一：图搜索（1页）

### 第7页：最短路的实现与作用
- 使用 Dijkstra（优先队列）计算节点间最短路。
- 对非法节点做边界检查；不可达路径返回空路径。
- 作用：所有策略的“距离依据”统一、可解释。

## 5. 核心技术二：调度策略（4页）

### 第8页：统一策略接口设计
- `DispatchStrategy::plan(vehicles, tasks, graph)` 返回 `DispatchDecision[]`。
- 好处：可插拔、可横向对比、低耦合。

### 第9页：Nearest 策略
- 对每个可分配任务找“最近空闲车”。
- 使用集合防止单辆车同轮重复被分配。
- 特点：快、直观、在轻载场景表现稳定。

### 第10页：Largest 策略
- 先按任务重量降序，再为每个任务选最近空闲车。
- 逻辑偏向“优先处理高权重任务”。
- 风险：可能增加整体绕路与局部饥饿。

### 第11页：RL 策略（DQN 推理接入）
- 先过滤可行候选车（空闲 + 电量可达）。
- 写状态文件 -> 调 Python 推理脚本 -> 读动作文件。
- 若推理异常/动作越界，自动 fallback 到 nearest。
- 工程价值：学习策略与规则策略共存，具备降级机制。

## 6. 核心技术三：充电管理（2页）

### 第12页：是否需要充电判定
- 估计“当前点 -> 目标 -> 仓库”总里程，乘安全系数。
- 按车辆 `max_range` 折算所需电量百分比。
- 电量不足则触发充电决策。

### 第13页：最优充电站选择与队列调度
- 站点代价 = 距离 + 等待时间 + 充电时间 + 负载率 + 队列长度（加权）。
- 到站后：有空桩则充电，无空桩则按优先级入队。
- 充完后释放充电桩并唤醒队列下一辆。

## 7. 仿真流程与数据生成（2页）

### 第14页：时间推进机制
- `tick(dt)` 推进当前时间，按 task_interval 触发任务生成。
- 任务数达到上限后停止生成，保证可控实验规模。

### 第15页：任务与车队初始化
- 任务到达：Poisson 分布（更贴近日常突发性）。
- 车辆异构：轻/中/重三类，电池和载重参数不同。
- 意义：避免“同质车辆”导致实验过于理想化。

## 8. 评分体系与指标（2页）

### 第16页：评分函数拆解
- 基础分 + 准时奖励/超时惩罚 + 路径质量 + 载重匹配 + 多车协同奖励。
- 最终分裁剪到[min,max]区间，保证数值稳定。

### 第17页：展示指标建议（老师最关心）
- 任务完成率
- 平均超时（分钟）
- 平均路径偏离率（actual/shortest）
- 充电等待时间与队列长度
- 单位时间得分

## 9. 实验设计（2页）

### 第18页：场景分级
- 小规模：少车少任务
- 中规模：任务密度提升
- 大规模：高并发 + 充电紧张
- 每种规模跑多次取均值，给出方差/误差棒。

### 第19页：消融与对照
- 策略对照：Nearest vs Largest vs RL。
- 模块消融：有/无充电队列；有/无 fallback；不同安全系数。
- 目标：回答“哪个模块真的有效”。

## 10. 结果分析（2页）

### 第20页：关键结果图表
- 柱状图：不同策略完成率与总分。
- 折线图：任务密度上升时延迟变化。
- 箱线图：不同策略的稳定性（方差）。

### 第21页：现象解释（不是只报数字）
- 为什么 RL 在复杂场景更稳/或不稳。
- 为什么 Largest 在部分高权任务场景有优势。
- 充电队列机制对系统连续运行的贡献。

## 11. 工程亮点与可扩展性（1页）

### 第22页：加分亮点（建议只讲 4 个最硬核）
1. **策略可插拔接口**：支持快速接入新策略。  
2. **学习策略有降级保护**：推理失败仍可运行。  
3. **充电决策是多因素综合成本，而非单纯最近站点**。  
4. **仿真-策略-评分闭环完整**：可复现实验、可迭代优化。

## 12. 局限与下一步（1页）

### 第23页：边界条件与未来工作
- 当前：路径速度模型较简化、路况静态、RL状态维度有限。
- 下一步：动态路况、多智能体协同、在线学习/重规划、真实地图接入。

## 13. 总结与Q&A（1页）

### 第24页：30秒收尾模板
- 我们做了什么（完整闭环系统）。
- 我们证明了什么（策略与机制有效性）。
- 我们还能走到哪里（工程可扩展）。

---

## C. 现场“可跳页”精简路径（老师没兴趣可直接切）

- **5分钟极速版**：第1 -> 5 -> 11 -> 20 -> 22 -> 24页
- **8分钟标准版**：第1~2 -> 5~7 -> 9~13 -> 18~21 -> 24页
- **12分钟完整版**：按顺序讲到24页

---

## D. 重点亮点（建议讲深，不要只念标题）

1. **我们不是只做算法，而是做“策略实验平台”**：统一接口，三策略可同框对比。  
2. **考虑了新能源核心痛点：电量 + 充电拥堵**，不是传统车辆调度简化版。  
3. **RL工程落地能力**：C++仿真主流程 + Python推理联动 + 失败降级。  
4. **评分函数多目标融合**：时间、距离、载重匹配、协同奖励都可量化。  
5. **异构车队建模**：轻中重车参数不同，更接近真实配送车队。  
6. **可视化演示与可复现实验并重**：展示效果好且能被验证。

---

## E. 重新整理的“代码细节型”答辩高频问题（老师按实现追问版）

> 这部分按“模块 -> 可能问题 -> 你们回答要点”给出。建议每题准备30~60秒答案。

## 1) Graph / 最短路模块

### Q1：你们为什么用 Dijkstra，不用 A*？
- 要点：当前图边权是非负且规模可控，Dijkstra实现简单稳定。
- 要点：策略层大量调用 shortest_path，先保证正确性与通用性；后续可用A*优化。

### Q2：如果节点 ID 越界会怎样？
- 要点：`shortest_path` 内先校验节点合法性，非法直接抛异常，避免静默错误。

### Q3：不可达节点如何处理？
- 要点：返回无效路径（空 Path），策略层可据此跳过不可行分配。

## 2) 调度策略模块

### Q4：三种策略怎么保证公平比较？
- 要点：同一仿真框架、同一任务流、同一评分函数，仅替换 `plan()` 实现。

### Q5：Nearest 为什么会“局部最优、全局不优”？
- 要点：它仅最小化当前任务距离，不显式优化后续任务与充电拥堵。

### Q6：Largest 为什么先按重量排序？会不会饿死小任务？
- 要点：目标是优先保障高重量任务收益，但确实可能延迟小任务；可加入deadline二级排序改进。

### Q7：你们如何避免一个车在一轮被分给多个任务？
- 要点：策略里用 `usedVehicles` 集合，单轮内车辆唯一分配。

## 3) RL 接入细节

### Q8：RL 状态是怎么构建的？
- 要点：状态文件包括任务信息（位置/重量/deadline）和候选车辆特征（距离、电量、载重等）。

### Q9：为什么 RL 里还要 fallback 到 nearest？
- 要点：真实工程中推理可能失败（脚本异常、输出越界）；fallback 保证系统持续可用。

### Q10：RL 可行性过滤逻辑是什么？
- 要点：车辆必须空闲，且电量要覆盖到任务点的估计消耗。

### Q11：C++ 与 Python 通信为什么用文件，不用 socket/RPC？
- 要点：课程项目优先稳健和低复杂度；文件方式易调试、可复现；后续可升级 IPC。

## 4) 充电管理模块

### Q12：什么时候判定“必须去充电”？
- 要点：估算“去目标+回仓”总距离（乘安全系数）转电耗，若当前电量不足则充电。

### Q13：选充电站为什么不是“最近即可”？
- 要点：最近不等于最快完成；我们综合距离、等待、充电时长、负载率、队列长度。

### Q14：排队优先级如何定义？
- 要点：电量越低优先级越高；估计充电时间短有加成；载重能力高也提高优先级。

### Q15：充完电后怎么处理等待队列？
- 要点：释放当前充电桩 -> 弹出队首请求 -> 分配给下一车辆 -> 更新其状态。

## 5) 仿真器与任务生成

### Q16：为什么任务到达用 Poisson 分布？
- 要点：更接近真实订单“随机到达、存在波峰波谷”的过程，比固定间隔更真实。

### Q17：如何保证实验可复现？
- 要点：固定随机种子、固定场景参数、统一运行时长与任务上限。

### Q18：你们是否考虑了异构车队？
- 要点：考虑了，轻中重车在最大载重、电池、续航都不同，影响调度与充电行为。

## 6) 评分与实验可信度

### Q19：评分函数里“最短路距离”怎么用？
- 要点：通过实际路径/最短路径比值衡量路径质量，偏离越大惩罚越明显。

### Q20：如何证明“充电管理模块”确实有贡献？
- 要点：做消融实验（去掉队列或改成最近站）比较总分、完成率、超时率。

### Q21：如何证明 RL 不是“碰巧更好”？
- 要点：多场景、多次运行，给均值+方差；并报告在高负载场景下的稳定性表现。

## 7) 工程质量与扩展

### Q22：如果规模扩大10倍，瓶颈在哪里？
- 要点：最短路高频调用与策略决策复杂度是主要瓶颈；可做缓存、增量更新、并行化。

### Q23：如果线上部署，还差什么？
- 要点：实时交通、地图服务、故障恢复、监控告警、在线模型更新。

### Q24：你们代码里最关键的“工程思维”是什么？
- 要点：模块解耦+统一接口+可降级策略，先保证可运行与可比较，再逐步提升最优性。

---

## F. 答辩时的表达模板（实用）

- **被质疑“为什么这么设计”**：
  - “我们先保障系统可运行与可比较，再用可插拔接口持续优化策略。”
- **被追问“代码在哪里体现”**：
  - “这一点在 shortest_path / scheduler / charging_manager 的实现链路里是贯通的。”
- **被追问“怎么证明有效”**：
  - “我们准备了对照+消融+多次重复实验，不只看单次最高分。”

